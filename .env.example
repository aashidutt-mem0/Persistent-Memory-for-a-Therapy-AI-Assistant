# Offline is deterministic and does not require credentials.
MEM0_DEMO_MODE=offline

# To mirror demo saves/searches to a real Mem0 project, set:
# MEM0_DEMO_MODE=live
# MEM0_API_KEY=your-mem0-api-key
